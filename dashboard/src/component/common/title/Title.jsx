import React from "react";

const Title = ({ title }) => {
  return (
    <>
      <div className="main-title">
        <h2>{title}</h2>
        <div class="heading-border"></div>
      </div>
    </>
  );
};

export default Title;
